@model IEnumerable<StoreFront.DATA.EF.Type>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IsMylar)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsLatex)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IsMylar)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsLatex)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TypeID })
        </td>
    </tr>
}

</table>
